"use client"

export default function Login() {
    /**
     * 登录
     */
    const login = () => {
        // console.log("userInfo=", userInfo)
    }



    return <button className={"login-btn"} onClick={login}>登录</button>
}